services:
  app:
    image: blou5/arg-app:latest
    ports:
      - "80:80"
      - "443:443"
    networks:
      - arg
    volumes:
      - ${PUBLIC_KEY}:/etc/ssl/aragosta.crt:r
      - ${PRIVATE_KEY}:/etc/ssl/aragosta.key:r
networks:
  arg:
    driver: bridge
